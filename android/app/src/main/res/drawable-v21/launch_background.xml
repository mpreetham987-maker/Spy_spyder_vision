<?xml version="1.0" encoding="utf-8"?>
<!-- See drawable/launch_background.xml — identical, v21 variant only
     exists because the default Flutter template splits these. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />
    <item android:width="14dp" android:height="14dp" android:gravity="center">
        <shape android:shape="oval">
            <solid android:color="@color/launch_mark" />
        </shape>
    </item>
</layer-list>
